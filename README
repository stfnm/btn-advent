A simple perl script which checks the BTN advent calendar for you.
The script won't perform any login, so you will have to provide an existing session ID cookie in the following file:

	PHPSESSID.txt

You can easily find out that cookie value within your browser.
